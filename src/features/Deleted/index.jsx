import React from 'react';
import PropTypes from 'prop-types';

Deleted.propTypes = {
    
};

function Deleted(props) {
    return (
        <div>
            
        </div>
    );
}

export default Deleted;